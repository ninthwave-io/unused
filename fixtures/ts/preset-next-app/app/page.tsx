// App Router convention file — invoked by Next.js by filename, never by an
// import edge. Must be kept alive by the "next" preset's entrypoint
// conventions (presets.ts's appRouterPatterns).
export default function Page(): JSX.Element {
  return <div>home</div>;
}
