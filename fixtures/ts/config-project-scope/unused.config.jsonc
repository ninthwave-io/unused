{
  // Narrow CLAIMABILITY to src/** only. scripts/** is still discovered and
  // parsed — its import edges still count toward reachability — it just can
  // never itself receive an "unused" claim (config.ts's "project vs ignore"
  // distinction: project narrows claimability, not visibility).
  "project": ["src/**"]
}
