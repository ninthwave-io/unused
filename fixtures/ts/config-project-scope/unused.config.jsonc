{
  // Narrow CLAIMABILITY to src/** only. scripts/** is still discovered and
  // parsed and its import edges are retained — it just can never itself
  // receive an "unused" claim. Only edges reached from a real root confer
  // liveness (ADR 0012).
  "project": ["src/**"]
}
