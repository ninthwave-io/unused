// Live infrastructure-as-code: instantiated by the CDK app entry (bin/app.ts).
// Reachable only through the CDK entrypoint the preset seeds, never a plain
// static import from elsewhere in the project.
export class MyStack {
  constructor() {}
}
