// Otherwise-unused exported class: never constructed, never imported for its
// value — the only mention anywhere is as a constructor-parameter type
// annotation in src/service.ts.
export class Dep {}
