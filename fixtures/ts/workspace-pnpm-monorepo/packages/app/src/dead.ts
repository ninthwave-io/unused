// Imported by nobody in @fix/app — a per-workspace dead file, sitting
// alongside the cross-workspace consumption mechanism this case exercises.
export function neverCalled(): string {
  return "dead";
}
