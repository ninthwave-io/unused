{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://unused.dev/schema/unused-config.schema.json",
  "title": "unused config",
  "description": "unused.config.jsonc / unused.config.json contract (PRD §6, ADR 0010). Hand-authored mirror of packages/unused/src/frontends/ts/config.ts's validateConfig — keep in lockstep. Shipped for editor autocomplete/validation only; the CLI's own hand-rolled validator (config.ts) is authoritative at runtime and produces the exit-3 error text (cli-ux §6).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "entry": {
      "$ref": "#/$defs/globArray",
      "description": "Glob patterns seeding ADDITIONAL production entrypoints, on top of zero-config auto-detection."
    },
    "entrySymbols": {
      "type": "array",
      "description": "Exact language-aware exported symbols seeded as production entrypoints without making sibling exports live.",
      "items": { "$ref": "#/$defs/entrySymbolRule" }
    },
    "project": {
      "$ref": "#/$defs/globArray",
      "description": "Ordered glob patterns narrowing claimability while preserving graph visibility. A leading ! excludes a prior match; a later positive can re-include it. Absent = every discovered file is claimable."
    },
    "suppressions": {
      "type": "array",
      "description": "Auditable claim suppression policies. Files remain fully analyzed and continue contributing graph edges.",
      "items": { "$ref": "#/$defs/suppressionRule" }
    },
    "ignoreDependencies": {
      "$ref": "#/$defs/globArray",
      "description": "Dependency names/patterns excluded from dependency-unused claims."
    },
    "workspaces": {
      "type": "object",
      "description": "Per-package overrides, keyed by the package's root-relative directory or its package.json name.",
      "additionalProperties": { "$ref": "#/$defs/workspaceOverride" }
    },
    "gate": { "$ref": "#/$defs/gate" },
    "presets": {
      "type": "array",
      "description": "Named framework presets (T4.4). Presence — even an empty array — forces exactly this set, overriding auto-detection.",
      "items": { "$ref": "#/$defs/preset" }
    },
    "ciSecondsPerTestFile": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Overrides the T5.3 zombie-test CI-seconds average (default 5) — docs/design/report-and-badge.md §3."
    }
  },
  "$defs": {
    "globArray": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "workspaceOverride": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "entry": { "$ref": "#/$defs/globArray" },
        "entrySymbols": {
          "type": "array",
          "items": { "$ref": "#/$defs/entrySymbolRule" }
        },
        "project": { "$ref": "#/$defs/globArray" },
        "suppressions": {
          "type": "array",
          "items": { "$ref": "#/$defs/suppressionRule" }
        }
      }
    },
    "entrySymbolRule": {
      "type": "object",
      "required": ["language", "file", "name", "reason"],
      "additionalProperties": false,
      "properties": {
        "language": { "type": "string", "enum": ["ts", "ex", "rs"] },
        "file": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?![A-Za-z]:)(?!.*\\\\)(?!.*(?:^|/)\\.{1,2}(?:/|$))(?!.*[*?{}\\[\\]])[^/]+(?:/[^/]+)*$"
        },
        "name": { "type": "string", "minLength": 1, "pattern": "\\S" },
        "reason": { "type": "string", "minLength": 1, "pattern": "\\S" }
      }
    },
    "suppressionRule": {
      "type": "object",
      "required": ["files", "kinds", "reason"],
      "additionalProperties": false,
      "properties": {
        "files": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "kinds": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "enum": ["export", "file", "dependency", "endpoint", "test"]
          }
        },
        "reason": { "type": "string", "minLength": 1, "pattern": "\\S" }
      }
    },
    "gate": {
      "type": "object",
      "required": ["threshold"],
      "additionalProperties": false,
      "properties": {
        "threshold": {
          "type": "string",
          "enum": ["high", "medium", "low"],
          "description": "The confidence floor `unused check` gates on (default high). Parsed/validated at M4; consumed by the CI gate at M7."
        }
      }
    },
    "preset": {
      "type": "string",
      "enum": ["vite", "next", "storybook", "cdk"]
    }
  }
}
