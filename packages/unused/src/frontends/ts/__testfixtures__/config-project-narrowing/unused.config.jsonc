{
  // "project" narrows CLAIMABILITY, not discovery (reviewer fix): scripts/
  // build.ts is outside this scope so it can never itself be claimed, but it
  // remains parsed and contributes its import edge for graph/counterfactual
  // analysis. The dead script is not promoted to a production root.
  "project": ["src/**"],
}
