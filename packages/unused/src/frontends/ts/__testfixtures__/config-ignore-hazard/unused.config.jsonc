{
  // Suppression is reporting policy only: src/loader.ts remains analyzed and
  // its computed-import hazard remains in the reference graph.
  "suppressions": [
    {
      "files": ["src/loader.ts"],
      "kinds": ["file", "export"],
      "reason": "runtime loader retained by policy",
    },
  ],
}
