// A Next.js metadata-route generator — invoked by filename convention, no
// import edge. Must stay alive.
export default function manifest(): { name: string } {
  return { name: "fixture-preset-next" };
}
