// A Next.js App Router page — reachable only via the next preset's
// entryPatterns (page.tsx is a route convention file), never imported by
// anything else in the project.
export default function HomePage(): string {
  return "home";
}
