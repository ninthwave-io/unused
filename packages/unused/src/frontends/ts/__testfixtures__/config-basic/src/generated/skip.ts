// Matched by a structured suppression policy: still parsed and claimed, with
// the claim hidden from default human output but retained in machine output.
export function generatedOrphan(): string {
  return "generated-orphan";
}
