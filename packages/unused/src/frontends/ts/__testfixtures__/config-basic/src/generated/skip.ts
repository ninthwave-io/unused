// Matched by "ignore": ["src/generated/**"] — undiscovered: never parsed,
// never claimed, even though it is also a genuine orphan.
export function generatedOrphan(): string {
  return "generated-orphan";
}
